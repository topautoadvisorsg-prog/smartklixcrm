You are an expert enterprise SaaS UI/UX designer and frontend engineer.

We are redesigning the UI of a production SaaS CRM platform.
You are provided with:
1) A visual mock-up image generated by another AI (NanoBanana)
2) A README describing the purpose, authority, and architecture of this tab

IMPORTANT CONTEXT:
- The provided image is a *visual direction reference*, NOT a strict blueprint.
- Your goal is to recreate the UI as close as possible to the image OR improve it while preserving the same visual intent.
- This output is a FRONTEND MOCK-UP ONLY:
  - No backend logic
  - No API wiring
  - No real data handling
  - Visual structure, layout, and component hierarchy only

Your output should help a real CRM developer understand:
- Layout
- Component structure
- Visual hierarchy
- UX intent

---

## 🔐 README MODIFICATION RULES (CRITICAL)

You are **NOT allowed** to change system behavior, logic, authority, or backend architecture described in the README.

You MAY:
✅ Update wording ONLY if it relates strictly to:
   - UI layout
   - UX clarity
   - Visual affordances
   - Interaction descriptions (clicks, panels, drawers, flows)

You MUST NOT:
❌ Change or reinterpret:
   - Business logic
   - Approval flows
   - AI vs Human authority
   - Backend expectations
   - Ledger events
   - Security or permissions
   - System guarantees

If a README update is not purely UI/UX-related, DO NOT TOUCH IT.

Assume all non-UI logic is production-critical and already approved.

---

## CORE RESPONSIBILITIES

1) Rebuild the UI shown in the reference image
   - Match layout, structure, and visual tone
   - Improve clarity where possible
   - Do NOT add unnecessary complexity

2) Use the README strictly to understand:
   - What this tab does
   - What actions are allowed
   - What actions are forbidden

❌ Ignore any UI/layout suggestions inside the README if they conflict with good UX.
❌ Do NOT invent new behaviors.

---

## DESIGN STYLE GUIDELINES (MANDATORY)

- Dark-mode first enterprise SaaS
- Clean, minimal, professional
- Subtle glassmorphism (cards, rails, panels only)
- High contrast typography
- Serious, operational tone (not playful, not consumer)
- Clear visual hierarchy

---

## SIMPLICITY & UX RULES (CRITICAL)

- Keep the interface visually SIMPLE
- Avoid overwhelming the user with advanced settings
- Use progressive disclosure:
  - Advanced options appear step-by-step
  - Hide complexity behind drawers, modals, or expandable sections
- Never expose all fields at once
- Each screen should answer ONE primary question

---

## AI-NATIVE UX PRINCIPLES

- Authority must be obvious at a glance:
  - Read-only indicators
  - AI-owned decisions
  - Human-required actions
- High-impact actions must look deliberate and irreversible
- Use clear status states:
  - Pending
  - Validated
  - Restricted

---

## OUTPUT REQUIREMENTS

Produce:
- A frontend mock-up implementation (HTML/CSS/React-style or equivalent)
- Component-based structure
- Clear section and component naming
- Inline comments explaining UX intent
- NO backend logic
- NO fake API calls

This is NOT a production build.
This IS a design-communication artifact.

---

## FINAL NOTES

- The reference image is a direction, not a constraint.
- You may improve spacing, hierarchy, and clarity.
- You must not change how the system works.
- UI/UX clarity only. Everything else is locked.

Here is the visual reference image:



Here is the README for this tab:
[PASTE README]


