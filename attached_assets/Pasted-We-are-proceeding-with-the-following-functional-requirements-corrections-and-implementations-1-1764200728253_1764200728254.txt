We are proceeding with the following functional requirements, corrections, and implementations.

✅ 1. CONTACTS MODULE FIXES
1.1. Fix search filtering (critical)

Search bar must perform real-time filtering (debounced)

Filter by: name, phone, email, tags

1.2. Add pagination

Server-side pagination for large datasets

1.3. Add sorting

By name, created date, status

1.4. Add status + tags filters

Tag multi-select filter

Contact status dropdown

1.5. Full CRUD for locations & equipment

Add Edit

Add Delete

Add ability to reorder (optional)

✅ 2. CONTACT DETAIL PAGE FIXES
2.1. Stripe Payment Method Integration

Implement Stripe Elements for adding payment methods:

Customer → Add Card

Tokenize with Stripe API

Save to backend under customer profile

2.2. Remove broken / placeholder functionality

Clean broken dialogs

Remove any non-implemented logic

2.3. Fix LSP errors

There are 4 diagnostics — clean them up.

✅ 3. ESTIMATES MODULE FIXES
3.1. Wire up “New Estimate” button

Behavior:

Open Estimate creation modal
OR

Redirect to EstimateDetail in “create” mode

3.2. Wire dropdown actions

View Details

Send to Customer (email)

Mark Accepted

Mark Rejected

3.3. Enable PDF export

Basic HTML → PDF wrapper acceptable.

3.4. Add Contact + Job columns to table
3.5. Implement public shareable view

/estimate/{id}/public

No authentication required

Read-only view

Matches G/B/B presentation layout

✅ 4. ESTIMATE DETAIL FIXES
4.1. Add core action buttons

Send

Accept

Reject

Convert to Invoice

4.2. Fix “Share URL”

Currently copies a dead link — must map to the new public route.

✅ 5. INVOICES MODULE FIXES
5.1. Add invoice creation

From table → “New Invoice”

5.2. Add email-send functionality

Send invoice via email

Include Stripe payment link

5.3. Add PDF export
5.4. Partial payments (optional, not required now)
✅ 6. PAYMENT MODULE FIXES

No refunds needed.

Implement:

Filter by date range

Filter by payment status

Filter by method (Card, ACH, Terminal)

Export is optional — skip unless trivial.

✅ 7. CALENDAR MODULE FIXES

Calendar will eventually sync with Google Calendar.

For now implement:

7.1. Add Week view
7.2. Add Day view
7.3. Clicking a day opens appointments list
7.4. Clicking an appointment opens detail popup

Not implementing drag-drop yet.

✅ 8. PIPELINE MODULE

Everything looks good.
Only add:

Column total values

Quick actions on cards (if simple)

Technician filter (optional)

✅ 9. PRICEBOOK
Add:

Cost field (for margin)

Unit of measure

CSV import/export (low priority)

GBB stays.

✅ 10. EMAILS MODULE

We accept that:

IMAP sync

SMTP send

…are NOT wired yet.

Add clear inline text:

“Email sync and send are handled via N8N. This page is for account configuration only.”

Leave as-is for now.

✅ 11. INTAKE BUILDER

Strip this down, keep it simple.

We DO need:

Link-based form

Field mapping

Submission storage

Ability to add photos (optional but useful)

We DO NOT need:

Embed code generator

Conditional logic

Complex form builder

Intake flow:

Customer opens link → fills → CRM auto-creates Contact + Job + Appointment.

✅ 12. SETTINGS
12.1. User Management (Critical)

Add:

Create user

Edit user

Delete user

Role assignment (Admin, Staff)

12.2. Tax Rate

Remove hardcoded 8.25%

Add tax field inside the Invoice form

Optional default tax inside Settings

12.3. Remove hard-coded elements

Avatar

Username

Sidebar labels

Everything must reflect logged-in user actual data.